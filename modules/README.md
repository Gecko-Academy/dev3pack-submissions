# The course moved

Everything under `modules/` now lives under `units/en/`, one directory per unit,
in the order `units/en/_toctree.yml` gives. Week 0's units are `units/en/w01-…`
to `w12-…`; the sessions are `units/en/session-01-…` to `session-15-…`.

If you had edited a week-0 notebook here, copy it across: the exercise ids and
your local progress are unchanged.
