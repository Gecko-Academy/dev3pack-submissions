# Quick quiz (ungraded)

### Q1: The model returns all four required fields plus `"source_url"`. What does `parse_research_answer` do?

<Question
  choices={[
    { text: "Keeps it; an extra field is free information", explain: "A field nobody asked for is a field nobody validates. A contract that accepts anything is not a contract." },
    { text: "Drops it silently and returns the other four", explain: "Silent is the problem. You would never learn the model is drifting away from the shape you asked for." },
    { text: "Rejects the whole payload, naming the unknown key", explain: "Correct. The gate is set equality, so it reports missing=[] unknown=['source_url'] and nothing half-validated gets through.", correct: true },
  ]}
/>

### Q2: The first reply fails to parse, the retry fails too. What comes back?

<Question
  choices={[
    { text: "A third attempt, with a stronger instruction", explain: "Retry until it works has no bound on cost or latency. A model that misread the instruction misreads it the fourth time too." },
    { text: "`None`, so the caller decides", explain: "That pushes the decision onto code with less context than the agent had, and every caller has to remember to check." },
    { text: "A ResearchAnswer with `needs_human_review: true`, empty citations, confidence 0.0", explain: "Correct. A typed refusal travels the same plumbing as a good answer and arrives where a person can see it.", correct: true },
  ]}
/>

### Q3: A reply parses cleanly and cites `internal-wiki`, which retrieval never returned. What happens?

<Question
  choices={[
    { text: "Nothing; it passed the schema", explain: "Schema conformance is necessary, never sufficient. The citation is a doc id the retriever never produced." },
    { text: "The citation is stripped, confidence is capped, and the answer is flagged", explain: "Correct. The application verifies citations against what retrieval actually returned; the trace records the strip.", correct: true },
    { text: "The parser raises AnswerParseError", explain: "The parser only knows the shape. It has never seen the retrieval result, so it cannot know that doc id is fabricated." },
  ]}
/>

### Q4: Your unsupported golden question is correct. What does its trace look like?

<Question
  choices={[
    { text: "retrieve returns nothing, then a decision line, and no llm_call at all", explain: "Correct. The refusal happens before the model is asked, so it costs nothing and cannot hallucinate.", correct: true },
    { text: "One llm_call, and the model says it does not know", explain: "That works too, and it spends a call to learn what retrieval already knew. Refuse before you spend." },
    { text: "Two llm_call lines, then a refusal", explain: "Two calls means the repair budget fired on a parse failure. That is a different failure from an unsupported question." },
  ]}
/>

### Q5: Why does the schema carry a `needs_human_review` field at all?

<Question
  choices={[
    { text: "For logging; downstream code ignores it", explain: "It is the one signal that a flagged refusal produces. Ignoring it is how a refusal gets read as an answer." },
    { text: "So refusal is expressible inside the contract", explain: "Correct. A schema that cannot say I don't know pushes the model into inventing, because every accepted shape claims to know.", correct: true },
    { text: "Because the provider's API requires it", explain: "The schema is yours. The provider returns text; every field in it is a choice you made and can test." },
  ]}
/>
