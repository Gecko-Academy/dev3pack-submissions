# Quick quiz (ungraded)

### Q1: What makes a class an `LLMClient`?

<Question
  choices={[
    { text: "It inherits from LLMClient", explain: "Nothing inherits from it. LLMClient is a Protocol; inheritance is not how it is satisfied." },
    { text: "It has a complete(system, user) -> str method, and nothing else is required", explain: "Correct. Structural typing: the method is the contract, so a four-line fake is as valid a client as a vendor adapter.", correct: true },
    { text: "It is registered in get_client", explain: "get_client maps configuration to a client. A class it has never heard of still satisfies the protocol." },
  ]}
/>

### Q2: You change `BOOTCAMP_PROVIDER` from unset to `ollama`. What is allowed to change?

<Question
  choices={[
    { text: "The wording of the answer, the latency, and whether two runs agree", explain: "Correct. Those are the model's. Permissions, grading, redaction, retry budgets and failure behaviour are yours and must not move.", correct: true },
    { text: "Which tools the model may call, because a better model can be trusted with more", explain: "A permission that depends on the model is not a permission. The lane must not widen it." },
    { text: "What happens on a failure, because a real provider fails differently", explain: "You will see different failures. What a failure does is your code's decision, and it stays the same." },
  ]}
/>

### Q3: The `anthropic` lane is configured and `ANTHROPIC_API_KEY` is empty. What happens?

<Question
  choices={[
    { text: "The request goes out and the API returns 401", explain: "Nothing leaves the machine. get_client raises before the SDK is even constructed." },
    { text: "get_client raises ConfigError naming .env.example, and never prints the key", explain: "Correct. It fails closed at configuration time, and a message that echoed the key would put a secret in a notebook output.", correct: true },
    { text: "It falls back to FakeLLM silently", explain: "The notebook preflight falls back and says so on its lane line. get_client itself raises; a silent downgrade would hide the missing key." },
  ]}
/>

### Q4: Your call to a provider exceeds its deadline. What should `answer_with_timeout` return?

<Question
  choices={[
    { text: "Nothing; let TimeoutError propagate so the caller sees the truth", explain: "The caller then handles two shapes and usually handles neither. ch02-e4 fails an escaped TimeoutError." },
    { text: "An AgentResult whose answer is flagged for review, cites nothing, and says the model did not respond in time", explain: "Correct. One shape either way, no citations for a call that never happened, and the flag downstream code routes on.", correct: true },
    { text: "A best-effort answer from the fake, so the pipeline keeps moving", explain: "That is the one dishonest ending: a plausible answer produced by something other than the model you asked." },
  ]}
/>

### Q5: The preflight prints `⚠️ lane = ollama, falling back to fake`. What is `LIVE`?

<Question
  choices={[
    { text: "A FakeLLM, and the printed line is the only honest statement of that", explain: "Correct. The probe could not reach the server or the model, so the notebook keeps running on the fake and says so.", correct: true },
    { text: "An OllamaClient that will raise on the first call", explain: "The lane check probes before it promises, so you get a client that works rather than one that fails later." },
    { text: "Undefined; the notebook stops there", explain: "The preflight never raises. A notebook that stops on a missing optional lane would block the required one." },
  ]}
/>
