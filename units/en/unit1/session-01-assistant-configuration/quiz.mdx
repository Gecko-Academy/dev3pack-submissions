# Quick quiz (ungraded)

### Q1: Which part of request → context → model → tool → verification does the model own?

<Question
  choices={[
    { text: "All five; the model decides everything", explain: "You control the context, the tools, and the verification. The model fills one gap." },
    { text: "Only the model step, and it is probabilistic there", explain: "Correct. Same question, two answers. Everything around it is deterministic and yours.", correct: true },
    { text: "Context and model", explain: "Context is the policy file and the files you name. That is yours to write." },
  ]}
/>

### Q2: Why does this repository keep the policy in `AGENTS.md` and make `CLAUDE.md` point at it?

<Question
  choices={[
    { text: "Claude Code cannot read a file called AGENTS.md", explain: "It reads whatever CLAUDE.md tells it to read. The pointer is a choice, not a limitation." },
    { text: "Duplicated policy drifts; one canonical file, per-assistant pointers", explain: "Correct. A rule that lives in three copies is three bugs waiting to disagree.", correct: true },
    { text: "AGENTS.md is shorter", explain: "It is the longest of the three. Length is not the reason." },
  ]}
/>

### Q3: The assistant proposes a plan that edits `tools.py`, its test, and `.github/workflows/pages.yml`. What do you do?

<Question
  choices={[
    { text: "Approve; the workflow change is probably useful", explain: "Probably useful is not in the allowed set. A plan naming an unnamed file is already a rejection." },
    { text: "Reject the workflow edit, approve the rest, record the rejection", explain: "Correct. The allowed files were named in the plan step. Everything outside them is out of scope until you say otherwise.", correct: true },
    { text: "Ask the assistant to grade its own diff", explain: "The assistant does not grade its own work. You run the check; you read the verdict." },
  ]}
/>

### Q4: Where does an acceptance check for a code change live?

<Question
  choices={[
    { text: "In the assistant's summary at the end of the task", explain: "A summary is a claim. AGENTS.md says never claim a command ran unless it ran." },
    { text: "In the Commands section of the policy file, as the exact command you also run", explain: "Correct. The command decides done, not the assistant's summary — here `uv run bootcamp check ch01`. The assistant proposes, the command decides, you approve.", correct: true },
    { text: "In a comment at the top of the changed file", explain: "A comment is not executed. The check has to run." },
  ]}
/>

### Q5: You gave the assistant "make search better" and it edited three files without asking. What is the homework?

<Question
  choices={[
    { text: "Revert and write a longer prompt next time", explain: "A prompt is forgotten by Thursday. The fix has to survive the session." },
    { text: "Add the sentence that would have made it ask to AGENTS.md", explain: "Correct. Instructions are code; an observed failure is a bugfix to the instruction file.", correct: true },
    { text: "Switch to a different model", explain: "The harness, not the model, made the difference. A different model with no boundary guesses the same way." },
  ]}
/>
